# 实验一糖尿病遗传风险监测 Windows开发与实验箱迁移规划

## 1. 实验需求

根据实验教程与实验书，本实验需要完成糖尿病风险二分类建模，输入为结构化 CSV 数据，输出测试集糖尿病风险概率与类别预测。

必须完成的内容：

1. 读取 `比赛训练集.csv` 和 `比赛测试集.csv`，检查文件编码、行列数、字段类型、标签分布、缺失值、重复编号和异常取值。
2. 仅基于训练集划分训练子集和验证子集，并且所有缺失值填补、异常值处理、编码和特征构造的统计量只从训练子集计算，避免数据泄漏。
3. 建立三个基分类模型：随机森林、LightGBM、CatBoost。
4. 在同一验证集上计算 Accuracy、Precision、Recall、F1、AUC，绘制指标比较图、ROC 曲线和混淆矩阵。
5. 提取三个模型的阳性预测概率，做等权软投票融合，以 0.5 为初始阈值生成类别预测，并比较融合模型与单模型表现。
6. 使用全部有标签训练数据重新训练三个基模型，对测试集输出风险概率和类别预测。
7. 保存程序、依赖清单、运行日志、模型评价指标、图表、测试集预测文件和实验报告材料。

当前数据概况：

| 文件 | 行数 | 列数 | 标签 |
| --- | ---: | ---: | --- |
| 比赛训练集.csv | 5070 | 10 | 含 `患有糖尿病标识` |
| 比赛测试集.csv | 1000 | 9 | 无标签 |

主要字段：

| 字段 | 处理要求 |
| --- | --- |
| 编号 | 样本唯一标识，不作为模型特征 |
| 性别 | 0/1 数值特征 |
| 出生年份 | 转换为年龄或保留为年份，建议构造年龄 |
| 体重指数 | 连续特征，0 值需按异常/缺失处理 |
| 糖尿病家族史 | 文本类别特征，编码为有、无、不确定三类 |
| 舒张压 | 连续特征，存在缺失值 |
| 口服耐糖量测试 | 连续特征，存在 -1 等异常值 |
| 胰岛素释放实验 | 连续特征，大量 0 值需作为异常/缺失候选 |
| 肱三头肌皮褶厚度 | 连续特征，大量 0 值需作为异常/缺失候选 |
| 患有糖尿病标识 | 训练集目标标签，0 为阴性，1 为阳性 |

已发现的数据问题：

| 问题 | 训练集 | 测试集 | 规划处理 |
| --- | ---: | ---: | --- |
| 舒张压缺失 | 247 | 49 | 训练子集中位数填补 |
| 体重指数为 0 | 8 | 2 | 视为缺失，中位数填补 |
| 口服耐糖量测试为负 | 249 | 16 | 视为缺失，中位数填补 |
| 胰岛素释放实验为 0 | 3478 | 684 | 视为缺失候选，并增加是否缺失指示列 |
| 肱三头肌皮褶厚度为 0 | 3432 | 689 | 视为缺失候选，并增加是否缺失指示列 |
| 编号重复 | 0 | 0 | 保留编号用于输出 |

## 2. Windows端开发规划

### 2.1 目录结构

建议在 Windows 上建立如下结构，迁移到实验箱时可原样复制：

```text
实验一_糖尿病遗传风险监测/
├─ 实验一_糖尿病遗传风险监测.py
├─ requirements.txt
├─ 数据/
│  ├─ 比赛训练集.csv
│  └─ 比赛测试集.csv
└─ 运行结果/
```

所有路径使用相对路径，不写 `C:\`、`D:\` 等 Windows 盘符。

Python 中统一使用：

```python
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent
DATA_DIR = BASE_DIR / "数据"
OUT_DIR = BASE_DIR / "运行结果"
OUT_DIR.mkdir(exist_ok=True)
```

### 2.2 Windows开发环境

推荐使用 Python 3.10 或 3.11，并建立独立虚拟环境：

```powershell
cd "C:\Users\YCL\Desktop\医学人工智能\实验一_糖尿病遗传风险监测"
python -m venv .venv
.\.venv\Scripts\activate
python -m pip install -U pip
python -m pip install -r requirements.txt
```

`requirements.txt` 最小依赖：

```text
numpy
pandas
scikit-learn
matplotlib
lightgbm
catboost
```

### 2.3 程序实现顺序

1. 数据读取与检查  
   验证 GBK 编码可读，输出训练/测试行列数、字段名、缺失值、重复编号、标签分布和异常值统计，保存为 `运行结果/数据检查记录.csv`。

2. 预处理管道  
   使用 `train_test_split(..., stratify=y, random_state=42)` 划分训练子集和验证子集。数值列用训练子集中位数填补，类别列用训练子集众数填补或 OneHot 编码。测试集只能复用训练集拟合出的处理规则。

3. 特征构造  
   将出生年份转换为年龄；对 BMI、口服耐糖量测试、胰岛素释放实验、皮褶厚度的异常/缺失增加指示列；糖尿病家族史编码为类别特征。

4. 三个模型训练  
   使用固定随机种子训练随机森林、LightGBM、CatBoost，记录主要参数。先用保守参数，避免实验箱 CPU 运行过慢。

5. 验证集评价  
   对三个模型分别输出概率和类别，计算 Accuracy、Precision、Recall、F1、AUC，保存 `模型评价指标.csv`、混淆矩阵图、ROC 曲线和指标比较图。

6. 软投票融合  
   对三个模型验证集阳性概率取平均，0.5 阈值转类别，计算同样指标并写入评价表。

7. 测试集预测  
   用全部训练集重新拟合预处理和三个模型，测试集输出至少三列：`编号`、`糖尿病风险概率`、`类别预测`。建议额外保留三个基模型概率，方便报告分析。

### 2.4 运行命令

Windows 调试：

```powershell
python "实验一_糖尿病遗传风险监测.py"
```

运行完成后应生成：

```text
运行结果/
├─ 数据检查记录.csv
├─ 模型参数记录.csv
├─ 模型评价指标.csv
├─ 测试集预测结果.csv
├─ 指标比较图.png
├─ ROC曲线.png
└─ 混淆矩阵_*.png
```

## 3. 实验箱迁移准备

### 3.1 迁移前自查

复制到 U 盘前检查：

1. 程序中没有 Windows 绝对路径。
2. CSV 文件位于 `数据/` 目录。
3. `requirements.txt` 与程序 import 一致。
4. 输出目录由程序自动创建。
5. 程序可以从空的 `运行结果/` 开始完整运行。
6. 中文文件名和 CSV 输出能正常打开。

### 3.2 实验箱运行流程

将完整文件夹复制到实验箱桌面后，在实验箱终端执行：

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda create -n course_dl --clone zhigouai
conda activate course_dl
cd "/home/bkrc/Desktop/实验一_糖尿病遗传风险监测"
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
python -c "import pandas, sklearn, lightgbm, catboost; print('实验一依赖检查通过')"
python "实验一_糖尿病遗传风险监测.py" 2>&1 | tee "实验一_运行日志.txt"
```

如果 `course_dl` 已存在，跳过 `conda create`，只执行：

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda activate course_dl
```

### 3.3 迁移风险与规避

| 风险 | 规避方式 |
| --- | --- |
| Windows 绝对路径导致实验箱找不到数据 | 全部用 `Path(__file__).resolve().parent` 拼接相对路径 |
| CSV 中文乱码 | 读取时优先指定 `encoding="gbk"`，输出用 `utf-8-sig` |
| LightGBM/CatBoost 安装较慢或失败 | Windows 先跑通；实验箱提前联网安装依赖；参数保持轻量 |
| 实验箱 CPU 慢 | 限制模型规模，例如随机森林树数、LightGBM 迭代数、CatBoost 迭代数不要过大 |
| 结果文件缺失 | 程序结束前统一检查输出文件是否存在、预测行数是否等于 1000 |
| 指标异常 | 检查概率范围 0 到 1、类别只含 0/1、验证集样本数一致 |

## 4. 最小验收标准

Windows 端验收：

1. 一条命令可完整运行程序。
2. `运行结果/模型评价指标.csv` 包含随机森林、LightGBM、CatBoost、软投票四行。
3. `运行结果/测试集预测结果.csv` 行数为 1000，编号无重复，概率范围为 0 到 1，类别只含 0 和 1。
4. 图表和 CSV 可正常打开。

实验箱端验收：

1. 终端显示 `(course_dl)`。
2. 依赖检查无 Traceback。
3. 运行日志 `实验一_运行日志.txt` 已保存。
4. `运行结果/` 中包含评价指标、图表和测试集预测文件。
5. 完整实验文件夹已复制回 U 盘。

## 5. 可暂不做的内容

先不做复杂自动调参、交叉验证堆叠融合、Web 界面或模型服务化。当前实验明确要求的是可复现实验流程、三模型比较、软投票融合、结果文件和报告材料；这些内容已经覆盖验收。
