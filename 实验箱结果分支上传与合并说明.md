# 实验箱结果分支上传与合并说明

## 1. 实验箱端上传结果

进入仓库：

```bash
cd medical-ai-lab-box
git pull origin main
```

新建结果分支：

```bash
git checkout -b lab-box-results
```

复制实验五结果：

```bash
mkdir -p exp5_xray_pneumonia/lab_outputs
cp exp5_xray_pneumonia/outputs/* exp5_xray_pneumonia/lab_outputs/
```

如果实验一也在实验箱重新运行了，复制实验一结果：

```bash
mkdir -p exp1_diabetes/lab_outputs
cp "实验一_糖尿病遗传风险监测/运行结果"/* exp1_diabetes/lab_outputs/
```

提交并推送：

```bash
git add .
git commit -m "Add lab box run results"
git push -u origin lab-box-results
```

如果提示没有 Git 用户名或邮箱，先执行：

```bash
git config user.name "YeChenglin8888"
git config user.email "你的GitHub邮箱"
```

再重新执行 `git commit` 和 `git push`。

## 2. PC端拉取并合并结果

回到 PC 后进入本地仓库：

```powershell
cd "C:\Users\YCL\Desktop\医学人工智能\医学人工智能_实验箱迁移包_clean"
git checkout main
git pull origin main
git fetch origin lab-box-results
git merge origin/lab-box-results
git push origin main
```

合并后检查结果文件：

```powershell
git status
dir exp5_xray_pneumonia\lab_outputs
```

## 3. 报告使用建议

报告中可对比：

1. PC 端 ViT 结果：`exp5_xray_pneumonia/outputs`
2. 实验箱端结果：`exp5_xray_pneumonia/lab_outputs`
3. 实验箱和 PC 的差异：设备、模型、训练轮数、运行时间、Accuracy、Precision、Recall、F1

实验箱端通常建议使用 `efficientnet_b0`，PC 端可使用 `vit_b_16`。
