# 两个实验运行说明

## 实验一 糖尿病遗传风险监测

Windows：

```powershell
cd "C:\Users\YCL\Desktop\医学人工智能\实验一_糖尿病遗传风险监测"
python -m pip install -r requirements.txt
python "实验一_糖尿病遗传风险监测.py"
```

实验箱：

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda activate course_dl
cd "/home/bkrc/Desktop/实验一_糖尿病遗传风险监测"
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
python "实验一_糖尿病遗传风险监测.py" 2>&1 | tee "实验一_运行日志.txt"
```

已生成结果位于：

```text
实验一_糖尿病遗传风险监测/运行结果/
```

## 实验五 胸部X光肺炎分类

实验箱若中文目录展开异常，优先使用英文目录：

```bash
cd medical-ai-lab-box/exp5_xray_pneumonia
```

图片数据已经放入英文目录：

```text
exp5_xray_pneumonia/data/
├─ train/NORMAL
├─ train/PNEUMONIA
├─ val/NORMAL
├─ val/PNEUMONIA
├─ test/NORMAL
└─ test/PNEUMONIA
```

中文目录若需手动放数据，结构如下：

```text
实验五_胸部X光肺炎分类/教学数据/
├─ train/NORMAL
├─ train/PNEUMONIA
├─ test/NORMAL
└─ test/PNEUMONIA
```

Windows：

```powershell
cd "C:\Users\YCL\Desktop\医学人工智能\实验五_胸部X光肺炎分类"
python -m pip install -r requirements.txt
python "实验五_胸部X光肺炎分类.py" --epochs 5 --batch-size 8
```

实验箱 CPU 较慢时：

```bash
source ~/miniconda3/etc/profile.d/conda.sh
conda activate course_dl
cd "/home/bkrc/Desktop/实验五_胸部X光肺炎分类"
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
python "实验五_胸部X光肺炎分类.py" --model efficientnet_b0 --epochs 3 --batch-size 4 2>&1 | tee "实验五_运行日志.txt"
```

如果实验箱时间允许，可用默认 ConvNeXt Tiny：

```bash
python "实验五_胸部X光肺炎分类.py" --epochs 5 --batch-size 8 2>&1 | tee "实验五_运行日志.txt"
```

英文目录运行：

```bash
cd "/home/bkrc/Desktop/medical-ai-lab-box/exp5_xray_pneumonia"
python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt
python run_exp5.py --model efficientnet_b0 --epochs 3 --batch-size 4 2>&1 | tee "exp5_run.log"
```

PC 有 CUDA 显卡时可跑 Vision Transformer：

```powershell
cd "C:\Users\YCL\Desktop\医学人工智能\医学人工智能_实验箱迁移包_clean\exp5_xray_pneumonia"
python run_exp5.py --model vit_b_16 --epochs 10 --batch-size 8
```
